ext {
    minSdkVersion = 14
    targetSdkVersion = 22
    compileSdkVersion = 25
    buildToolsVersion = "25.0.3"
    supportLibraryVersion = "25.3.1"
    supportLibs = [
        appCompat : "com.android.support:appcompat-v7:${supportLibraryVersion}",
        design : "com.android.support:design:${supportLibraryVersion}",
        supportV13 : "com.android.support:support-v13:${supportLibraryVersion}"
    ]
    googlePlayServicesVersion = "10.2.6"
    gpsLibs = [
        ads : "com.google.android.gms:play-services-ads:${googlePlayServicesVersion}",
        analytics : "com.google.android.gms:play-services-analytics:${googlePlayServicesVersion}",
        location : "com.google.android.gms:play-services-location:${googlePlayServicesVersion}",
        maps : "com.google.android.gms:play-services-maps:${googlePlayServicesVersion}",
        firebase_ads : "com.google.firebase:firebase-ads:${googlePlayServicesVersion}",
        firebase_analytics : "com.google.firebase:firebase-analytics:${googlePlayServicesVersion}",
        firebase_crash : "com.google.firebase:firebase-crash:${googlePlayServicesVersion}",
    ]
    facebookSdkVersion = "4.23.0"
    facebookMediationSdkVersion = "${facebookSdkVersion}.0"
    fbLibs = [
        audienceNetwork : "com.facebook.android:audience-network-sdk:${facebookSdkVersion}",
        audienceNetworkMediation : "com.google.ads.mediation:facebook:${facebookMediationSdkVersion}"
    ]
    leakCanaryVersion = "1.5.1"
    squareLibs = [
        leakcanary : "com.squareup.leakcanary:leakcanary-android:${leakCanaryVersion}",
        leakcanary_no_op : "com.squareup.leakcanary:leakcanary-android-no-op:${leakCanaryVersion}"
    ]
}

